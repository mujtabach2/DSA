public interface Drawable {
    public void draw();
}

// interface always hold methods that have to be implemented in the classes
// no construstors required, All of the methods in an interface are abstract.
// cannot create an object of an interface
// interface is a contract that the class has to follow
// interface is a way to achieve multiple inheritance
// can only hold static variable (constant)